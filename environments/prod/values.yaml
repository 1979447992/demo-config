replicaCount: 3

image:
  tag: main

service:
  type: LoadBalancer

env:
  NODE_ENV: production
  APP_VERSION: "1.0.0-prod"

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi
