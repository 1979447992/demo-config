replicaCount: 2

image:
  tag: main

service:
  type: LoadBalancer

env:
  NODE_ENV: staging
  APP_VERSION: "1.0.0-staging"

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
